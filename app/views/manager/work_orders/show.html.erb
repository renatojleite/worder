<div class="wrapper">
  <div id="content">
  <!-- <nav class="navbar navbar-expand-lg navbar-light bg-light"> -->
      <button type="button" id="sidebarCollapse" class="btn btn-info">
      <i class="fas fa-align-left"></i>
      <span>Toggle Sidebar</span>
      </button>
  <!-- </nav> -->
  <%= render 'sidebar' %>
  </div>

<div class="container-full content-master">
  <div class="container">
    <div class="row">
      <div class="col">
        <div
          id="map"
          style="width: 100%;
          height: 300px;
          margin-left: 100px;"
          data-marker="<%= @marker.to_json %>"
          data-mapbox-api-key="<%= ENV['MAPBOX_API_KEY'] %>">
        </div>
      </div>
    </div>
  </div>
</div>


<%= cl_image_tag @work_order.photo, height: 300, width: 400, crop: :fill %>
<%= @work_order.report %>
<%#= cl_image_path @work_order.photo_url, height: 300, width: 400, crop: :fill %>
