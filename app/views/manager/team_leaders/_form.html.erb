<%= simple_form_for @team_leader do |f| %>
  <%= f.input :team_name %>
  <%= f.button :submit, class: "btn-primary" %>
  <%= f.button :button, "Cancel", type: "reset", class: "btn-outline-secondary" %>
<% end %>
