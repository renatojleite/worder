<%= simple_form_for @team_leader, url: manager_team_leaders_path do |f| %>
  <%= f.input :team_name %>
  <%= f.association :user %>
  <%= f.button :submit, class: 'btn btn-primary' %>
<% end %>


<%#= f.association :user, collection: User.where(admin: false) %>
